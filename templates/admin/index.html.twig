{% extends 'backbase.html.twig' %}

{% block title %}Hello AdminController!{% endblock %}

{% block body %}
     {{ form_start(form) }}
          {{ form_row(form.Users, {'attr': {'class': 'form-control form-control-user'}}) }}
          {{ form_row(form.Roles, {'attr': {'class': 'form-control form-control-user'}}) }}
          <button type="submit" class="btn btn-primary">Update</button>
     {{ form_end(form) }}
{% endblock %}

