{{ form_start(form) }}
    {# { { form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button> #}

        {{ form_row(form.prix, {'attr': {'class': 'form-control form-control-user'}}) }}
        {{ form_row(form.quantite, {'attr': {'class': 'form-control form-control-user'}}) }}
        {{ form_row(form.description, {'attr': {'class': 'form-control form-control-user'}}) }}
        {{ form_row(form.Nomproduit, {'attr': {'class': 'form-control form-control-user'}}) }}
        {{ form_row(form.categorie, {'attr': {'class': 'form-control form-control-user'}}) }}


    <button type="submit" class="btn btn-primary">Save</button>
{{ form_end(form) }}
